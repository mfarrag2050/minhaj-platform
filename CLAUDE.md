# قواعد الكود لمنصّة منهاج

هذا الملف يُقرأ من كل جلسة عمل. الالتزام به إلزامي.

## Security — non-negotiable

هذا القسم **إلزامي**. أيّ خرق منه يُرفض في المراجعة، ولا يُعالج بـ«سنصلحه لاحقاً». المنصّة تحمل بيانات قاصرين، والقاعدة الافتراضيّة هي الرفض ثم إثبات الأمان — لا العكس.

1. **كل استعلام قاعدة بيانات عبر `$wpdb->prepare()`.** لا استيفاء (interpolation) نصوصٍ في SQL على الإطلاق، حتى ولو بدت المدخلات «آمنة». أسماء الجداول تُقرأ من ثوابت أو من `$wpdb->prefix` — لا من `$_GET` ولا `$_POST` ولا من أيّ مصدر خارجيّ.
2. **كل action/endpoint يجمع فحصين معاً: `current_user_can()` + nonce.** الاعتماد على إخفاء الواجهة (زر غير معروض، رابط غير مُعلَن) **ليس** ضابط صلاحيّة. الفحصان يقعان قبل أيّ side effect.
3. **REST API: `permission_callback` إلزاميّ**، ولا يكون `__return_true` أبداً — ولا حتى مؤقّتاً. المسار الذي لا يملك سياسة صلاحيّة صريحة لا يُسجَّل.
4. **لا تثق بأيّ معرِّف قادم من الطلب.** ملكيّة المورد (طالب، حلقة، درجة، وليّ أمر) تُتحقَّق من جلسة المستخدم في كل عمليّة قراءة أو كتابة. **هذا بالضبط ما أسقط LearnDash: IDOR عبر معرِّفات مباشرة بلا فحص ملكيّة.** إن كان المستخدم يستطيع طلب `id=123`، فعلى الشيفرة أن تثبت أنّ `123` تخصّه أو أنّ دوره يخوّله ذلك.
5. **تعقيم عند الإدخال، هروب عند الإخراج، بلا استثناء.** `sanitize_text_field`/`sanitize_email`/`absint`/`wp_kses_post` عند الاستلام، و`esc_html`/`esc_attr`/`esc_url`/`esc_js` عند الطباعة. لا تعقيم بدل الهروب ولا العكس — كلٌّ في موضعه.
6. **`defined( 'ABSPATH' ) || exit;`** في أوّل كلّ ملفّ PHP قبل أيّ كود آخر.
7. **لا أسرار في المستودع.** المفاتيح ورموز الوصول تُقرأ من ثوابت `wp-config.php` أو من ملفّ env خارج جذر الويب بصلاحيّة `600`. لا `.env` مكشوف، لا مفاتيح في التعليقات، لا رموز في الاختبارات.
8. **رفع الملفّات:** نوع MIME يُستنتَج من **محتوى** الملفّ (`finfo`/`wp_check_filetype_and_ext`) لا من امتداده أو من رأس `Content-Type` الذي يرسله العميل. الملفّات المرفوعة تُخزَّن **خارج** المسارات القابلة للتنفيذ، وباسم مولَّد لا باسم أرسله المستخدم.
9. **تسجيل تدقيق (audit log)** لكلّ عمليّة تمسّ بيانات قاصر أو تُعدِّل صلاحيّة: من فعل، ماذا فعل، متى، على أيّ مورد. السجلّ يُكتَب في جدول مخصّص لا يُعدَّل من واجهة الاستخدام.

الفحوصات الآليّة التي يفرضها `phpcs.xml.dist` (WordPress-Extra + WordPress.Security كأخطاء) هي **الحدّ الأدنى**، لا سقف الأمان.

## اللغة والمعيار
- **PHP 8.2 أو أعلى.** لا كود يعمل على أقلّ من ذلك.
- **WordPress Coding Standards (WPCS)** مطبَّقة على كل ملفّ PHP. يُفحص المستودع عبر `composer phpcs`.
- **الفضاء الاسمي الجذر:** `Minhaj\`. كل صنف يقع تحت هذا الجذر، ويتبع مسار المجلّد داخل `plugins/minhaj-core/includes/` بحسب PSR-4.

## بنية الإضافة
- كل الكود يعيش داخل إضافة `plugins/minhaj-core`. **لا** ملفّات `mu-plugins`، **لا** `functions.php` للقالب، **لا** code snippets عبر مكوّنات خارجية.
- كل وحدة عمل (module) تسكن `includes/modules/<اسم-الوحدة>/` ولها فضاء اسمي `Minhaj\Modules\<Name>\`.
- الوحدات تُسجَّل مركزيّاً من مُشغّل الإضافة، ولا تُحمَّل عبر `require` مباشر خارج الـautoloader.

## قاعدة البيانات
- **جداول مخصّصة فقط.** ممنوع استخدام Custom Post Types لتمثيل كيانات المجال (الطلاب، الحلقات، الجدول، إلخ).
- الجداول تُنشأ وتُعدَّل عبر نظام migrations الموجود في الإضافة، ولها بادئة `$wpdb->prefix . 'minhaj_'`.
- جدول `minhaj_schema_versions` يسجّل رقم آخر مخطّط مطبَّق. لا تُنفَّذ migration مرّتين.
- كل استعلام مكتوب يدويّاً يمرّ عبر `$wpdb->prepare()`. لا concatenation مع مدخلات المستخدم.

## الترجمة (i18n)
- المشروع بستّ لغات. **كل نصّ** يظهر للمستخدم يمرّ عبر `__()` أو `_e()` أو `esc_html__()` بنطاق `minhaj-core`.
- لا نصوص بلغة واحدة داخل الشيفرة. لا تخلط بين الفارسي/الأردو/العربية/الإنجليزية/التركية/الفرنسية داخل السلاسل — كلّها تُدار عبر ملفّات `.po/.mo`.

## هيكل الأصناف
- الأصناف تتبع مبدأ المسؤوليّة الواحدة؛ الملفّ باسم الصنف بحرف كبير أوّل (`PascalCase.php`).
- الدوالّ الحرّة تُتجنَّب — نستخدم أصنافاً مع dependency injection حين يمكن.
- لا `global` إلا `$wpdb` وعند الحاجة الحقيقيّة.

## ما لا يُكتب الآن
- وحدة الجدول الأسبوعيّ (Timetable) ووحدة المجموعات لم تُكتب بعد؛ تنتظر المواصفة النهائيّة.
- لا تُنشئ أصنافاً استباقيّاً لهذه الوحدات.
